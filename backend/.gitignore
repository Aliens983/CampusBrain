# ===== 后端项目文档（个人笔记，不入库）=====
docs/

# ===== Maven 构建产物 =====
target/
*.class

# ===== 日志 =====
logs/
*.log
*.out

# ===== IDE =====
.idea/
.vscode/
*.iml
*.iws
*.ipr

# ===== 环境变量 / 密钥 =====
.env
.env.local
.env.*.local
*.pem
*.key

# ===== 应用配置：已全环境变量化（${VAR}），无真实密钥，可入库 =====
# 真实密钥只在 backend/.env（gitignore）与部署环境变量里提供

# ===== 运行时上传/临时文件（不入库）=====
uploads/
data/
*.tmp

# ===== 操作系统 =====
.DS_Store
Thumbs.db
file/

# ===== 需入库的默认素材（已 commit 跟踪，跟踪状态不受 ignore 影响）=====
# 注意：不要加 “!uploads/” 顶层反选，否则会把 uploads 下的运行时散图也放开暴露。
!uploads/campus/
!uploads/campus/cq.jpg
!uploads/campus/xs.jpg

