<?xml version="1.0" encoding="UTF-8"?>
<!--
    单元测试专用日志配置（logback 原生自动发现：测试类路径下本文件优先于 Spring 的
    logback-spring.xml；纯 Mockito 单测不启动 Spring，后者本来也不会被原生 logback 加载，
    此前回落到默认 BasicConfiguration=DEBUG，负向测试故意注入的异常堆栈全部刷进 CI 控制台，
    红色 ERROR 易被误判为构建失败）。

    口径：
    - com.kb 全部 OFF：单测均为 Mock/桩驱动，行为靠断言验证；负向测试「故意打故障
      验证降级」（Qdrant/Redis/ES/LLM 不可用、断路器开、删除重试耗尽等）打印的
      ERROR/WARN 全是预期产物，静默不影响断言；本地调试时把该 logger 临时调到 DEBUG 即可。
    - root=WARN：仅兜第三方组件的意外告警。
-->
<configuration>
    <appender name="TEST_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <logger name="com.kb" level="OFF"/>

    <root level="WARN">
        <appender-ref ref="TEST_CONSOLE"/>
    </root>
</configuration>
