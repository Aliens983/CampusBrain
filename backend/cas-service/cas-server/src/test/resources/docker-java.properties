# docker-java（Testcontainers 底层）配置
# 本机/CI 的 Docker daemon 最低仅接受 API 1.40（Docker 19.03+，2019 年起），
# docker-java 内置默认 1.32 会被 daemon 以 400 拒绝导致集成测试被跳过。
# 固化在测试 classpath，避免依赖 DOCKER_API_VERSION 环境变量。
api.version=1.40
